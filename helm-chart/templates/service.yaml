apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-cd-test.fullname" . }}
  labels:
    {{- include "argo-cd-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "argo-cd-test.selectorLabels" . | nindent 4 }}
